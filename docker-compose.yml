version: '3.8'

services:
  app:
    build: .
    ports:
      - "4000:8080"
    volumes:
      - ./data:/app/data
    environment:
      - ADMIN_EMAIL=admin@unne.edu.ar
      - ADMIN_PASSWORD=admin123
    restart: unless-stopped
